"use client";
import {useState} from "react";
export default function DistrictComparisonModal(){const [open,setOpen]=useState(false),[codes,setCodes]=useState("8004,3011,6300"),[items,setItems]=useState<Record<string,unknown>[]>([]);async function load(){const b=process.env.NEXT_PUBLIC_API_URL??"http://127.0.0.1:8310";const r=await fetch(`${b}/api/v1/districts/compare?postcodes=${encodeURIComponent(codes)}`);if(r.ok)setItems((await r.json()).items)}return <><button className="min-h-11 rounded border px-3" onClick={()=>setOpen(true)}>Vergleichen</button>{open&&<div role="dialog" aria-modal="true" aria-label="District comparison" className="fixed inset-0 z-50 grid place-items-center bg-black/70"><div className="max-h-[85vh] w-[min(900px,94vw)] overflow-auto rounded-xl bg-slate-950 p-5"><button onClick={()=>setOpen(false)} aria-label="Close">×</button><input aria-label="Postcodes" value={codes} onChange={e=>setCodes(e.target.value)} className="m-2 bg-slate-800 p-2"/><button onClick={load}>Load</button><pre className="text-xs">{JSON.stringify(items,null,2)}</pre></div></div>}</>}

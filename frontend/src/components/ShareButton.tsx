"use client";import {useState} from "react";import {useTranslations} from "next-intl";export default function ShareButton({getUrl}:{getUrl:()=>string}){const t=useTranslations("share"),[s,setS]=useState("");async function copy(){try{await navigator.clipboard.writeText(getUrl());setS(t("copied"))}catch{setS(t("error"))}}return <span><button data-testid="share-button" onClick={copy} className="min-h-11 rounded border px-4">{t("button")}</button><span role="status" aria-live="polite">{s}</span></span>}

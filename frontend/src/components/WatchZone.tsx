"use client";import {useEffect,useState} from "react";import {useTranslations} from "next-intl";export default function WatchZone({center,radius,onRadiusChange}:{center?:[number,number]|null;radius:number;onRadiusChange:(n:number)=>void}){const t=useTranslations("watch"),[count,setCount]=useState<number|null>(null);useEffect(()=>{if(!center)return;const c=new AbortController();fetch(`${process.env.NEXT_PUBLIC_API_URL??"http://localhost:8310"}/api/v1/planning/radius?lat=${center[1]}&lon=${center[0]}&radius_m=${radius}&active_only=true`,{signal:c.signal}).then(x=>x.ok?x.json():Promise.reject()).then(x=>setCount(x.items.length)).catch(()=>setCount(null));return()=>c.abort()},[center,radius]);return <section data-testid="watch-zone" className="flex flex-wrap items-center gap-2 p-3"><b>{t("title")}</b>{[300,500,1000].map(n=><button className="min-h-11 rounded border px-3" aria-pressed={n===radius} key={n} onClick={()=>onRadiusChange(n)}>{n} m</button>)}<output aria-live="polite">{!center?t("selectPlace"):count===null?t("loading"):t("count",{count})}</output></section>}
